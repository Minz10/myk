<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myk">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.WRITE_SMS"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_myk"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_myk"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyK">
        <activity android:name=".Signup"></activity>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"> </activity>
        <activity android:name=".Temperature"></activity>
        <activity android:name=".LPG"></activity>
        <activity android:name=".Contact"></activity>
        <activity android:name=".Profile"></activity>
        <activity android:name=".Tips"></activity>
    </application>

</manifest>